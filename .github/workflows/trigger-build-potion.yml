name: Trigger potion script build

on:
  issues:
    types: [opened]

permissions:
  contents: write

jobs:
  build:
    if: github.event.issue.title == 'build-potion-script'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Extract and run builder
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import textwrap